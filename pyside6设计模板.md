# PySide6 设计模板

## 1. 适用范围

本模板用于设计基于 `PySide6` 的本地桌面工具界面，适合以下类型的项目：

- 本地管理工具
- 文件管理工具
- 数据查看与操作工具
- 需要左侧列表 + 右侧详情 / 功能侧栏的桌面应用
- 需要在 Windows 环境下稳定运行的 `Python GUI`

本模板的目标不是做“好看但难用”的界面，而是做：

- 结构清晰
- 可压缩
- 可扩展
- 响应式稳定
- 日常操作效率高

## 2. 总体设计原则

### 2.1 先保证结构，再谈视觉

优先保证以下几点：

- 各区域职责清晰
- 窗口缩小时不会挤爆
- 常用操作路径短
- 状态刷新后界面不会乱跳

视觉优化建立在结构正确的前提上。

### 2.2 默认按“工具型 UI”设计

对于桌面工具，不优先模仿网页，也不优先堆视觉卡片，而是优先：

- 信息密度适中
- 关键按钮易点
- 列表可快速浏览
- 操作反馈明确

### 2.3 一开始就考虑缩放与压缩

不要先做一个只在大窗口里好看的布局，再补“缩小时怎么办”。

应在设计开始时就考虑：

- 默认尺寸
- 最小尺寸
- 缩小时哪些区块换行
- 缩小时左右布局是否切上下布局
- 哪些区域需要滚动容器

## 3. 推荐布局模板

推荐优先采用以下结构：

1. 顶部配置区
2. 中部筛选区
3. 操作按钮区
4. 主内容区

主内容区推荐再拆成：

- 左侧主列表区
- 右侧功能 / 状态侧栏

### 3.1 顶部配置区

适合放：

- 连接参数
- 全局配置
- 账号信息
- bucket / 项目切换

设计建议：

- 做成可折叠区域
- 如果本地已有完整配置，默认折叠
- 展开时使用多列表单
- 缩小时自动从 3 列降到 2 列，再降到 1 列

### 3.2 筛选区

适合放：

- 搜索框
- 前缀筛选
- 状态筛选
- 刷新按钮

设计建议：

- 与主配置区分开
- 缩小时允许按钮换到下一行
- 不要和对象操作按钮混在一起

### 3.3 操作按钮区

适合放：

- 上传
- 下载
- 删除
- 生成链接
- 创建分享

设计建议：

- 独立成一个区域
- 默认一行排布
- 缩小时按网格自动换行
- 危险操作使用独立样式

### 3.4 主内容区

优先使用 `QSplitter`。

宽屏时：

- 左右分栏

窄屏时：

- 自动切换为上下分栏

设计建议：

- 左侧给主列表更多空间
- 右侧做成侧栏，不要喧宾夺主
- 右侧放入 `QScrollArea`

## 4. 响应式设计模板

### 4.1 必须支持的响应式行为

至少应支持以下行为：

- 配置区自动换列
- 按钮区自动换行
- 主内容区横向分栏切换为纵向分栏
- 右侧侧栏内容允许滚动
- 不因窗口缩小出现整块内容被裁切不可见

### 4.2 推荐断点思路

可以按窗口宽度设置 3 档：

- 大窗口：完整横向布局
- 中窗口：部分区域换行
- 小窗口：主内容区改上下布局

推荐不要把断点写死成视觉结论，而是按组件实际拥挤程度调整。

### 4.3 哪些内容不应强行固定

以下内容不要写死：

- 表单永远 3 列
- 所有按钮永远一行
- 右侧侧栏永远固定宽度
- 圆环、卡片永远固定尺寸

这些都会让界面在小窗口下失真。

## 5. 折叠区模板

适合做折叠的内容：

- 连接配置
- 高级设置
- 不需要一直盯着看的参数区

推荐做法：

- 用独立组件封装，例如 `collapsible_section.py`
- 点击标题即可展开或折叠
- 标题区要有箭头状态
- 默认展开还是折叠应按使用频率决定

经验规则：

- 频繁修改的区域默认展开
- 只在启动时或偶尔修改的区域默认折叠

## 6. 列表 / 表格设计模板

### 6.1 列表是主视图时的原则

如果左侧表格已经展示了对象基础信息，就不要在右侧重复做一份完全相同的详情卡片。

右侧只保留真正补充性的内容，例如：

- 分享链接
- URL
- 容量
- 统计
- 当前选中对象的扩展操作

### 6.2 表头排序必须作为一等能力

如果表格是主工作区，则默认应支持：

- 点击表头排序
- 升序 / 降序切换
- 刷新后保留当前排序状态

### 6.3 排序实现原则

不要只按显示文本排序。

应按真实值排序，例如：

- `Size` 按字节数排序
- 数值列按整数排序
- 文本列按标准字符串排序
- 日期列按可比较时间值排序

建议做法：

- 自定义 `QTableWidgetItem`
- 单独存 `sort_value`
- `__lt__` 中按真实值比较

### 6.4 选中状态原则

表格刷新后，尽量保留：

- 当前选中项
- 当前排序状态
- 当前滚动位置（如果有必要）

不要每次刷新都把用户操作上下文打断。

## 7. 右侧侧栏设计模板

右侧侧栏适合保留：

- 当前可复制的 URL
- 当前可复制的分享链接
- 容量概览
- 状态摘要

不建议保留：

- 与左侧完全重复的对象基础字段

设计建议：

- 尽量精简
- 一张卡片只承担一种职责
- 内容多时优先纵向堆叠，不强行横向塞按钮

## 8. 视觉样式模板

### 8.1 推荐风格

适合工具型桌面应用的风格：

- 浅背景
- 白色内容卡片
- 柔和边框
- 圆角适中
- 主按钮有明确主色
- 危险按钮有单独警示色

### 8.2 视觉优先级

优先统一以下内容：

- 字号层级
- 间距
- 按钮圆角
- 输入框圆角
- 表头样式
- 选中态颜色

建议统一放到：

- `theme.py`
- `APP_STYLESHEET`

## 9. 后台任务与交互模板

### 9.1 耗时操作必须异步

以下操作默认不要阻塞主线程：

- 网络请求
- 上传
- 下载
- 删除
- 远程状态查询
- 大列表刷新

### 9.2 推荐结构

建议统一封装到：

- `tasks.py`

让界面层只负责：

- 发起任务
- 禁用按钮
- 更新状态栏
- 接收成功或失败回调

### 9.3 关闭窗口时的稳定性

如果后台任务仍在执行，关闭窗口时容易出现信号对象被释放的问题。

推荐做法：

- 任务对象保留活动引用
- 发信号时做安全封装
- 避免窗口关闭时控制台刷异常

## 10. 状态与反馈模板

### 10.1 反馈分层

建议分三层：

- 状态栏：普通状态变化
- 轻提示：操作完成但不需要打断用户
- 弹窗：严重错误或确认删除

### 10.2 不建议的做法

不要把所有反馈都做成弹窗。

否则用户在频繁操作时会非常割裂。

## 11. 推荐文件结构模板

推荐结构如下：

```text
.
├── app.py
├── requirements.txt
├── config_manager.py
├── r2_client.py
├── worker_client.py
└── pyside6_ui/
    ├── main_window.py
    ├── state.py
    ├── tasks.py
    ├── theme.py
    └── widgets/
        ├── collapsible_section.py
        ├── object_table.py
        ├── detail_panel.py
        └── storage_ring.py
```

## 12. 可直接复用的设计检查清单

每次新做 `PySide6` UI 时，都可以按下面检查：

### 12.1 布局

- 配置区是否应该折叠
- 主列表和侧栏职责是否清晰
- 窗口缩小时是否会挤爆
- 是否需要横向切纵向布局
- 哪些区域需要滚动

### 12.2 表格

- 是否支持表头排序
- 是否按真实值排序
- 刷新后是否保留排序状态
- 是否保留选中项

### 12.3 交互

- 耗时操作是否异步
- 操作期间按钮是否禁用
- 是否有清晰状态反馈
- 关闭窗口时是否会报线程异常

### 12.4 文档

- 是否把实际断点和布局策略写清楚
- 是否把可折叠区域写清楚
- 是否把排序逻辑写清楚
- 是否把右侧侧栏应保留什么、不应保留什么写清楚

## 13. 一句话模板

以后如果要快速定义一套新的 `PySide6` 工具型 UI，可以默认套用这一句：

“采用 `顶部可折叠配置区 + 中部筛选区 + 操作按钮区 + 左侧主表格 + 右侧功能侧栏` 的结构，支持窗口缩小时自动换列、按钮换行、主内容区横竖切换，并为主表格提供可点击表头排序与刷新后状态保持能力。”
