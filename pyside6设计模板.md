# PySide6 设计模板

## 1. 模板定位

本模板用于以后设计新的 `PySide6` 本地桌面工具 UI。它不是某一个项目的临时样式说明，而是一套可复用的界面设计规范。

默认设计目标是：

- 美观，但不牺牲信息密度
- 有 Apple 风格的克制、留白、圆角和层级感
- 仍然像专业桌面工具，能承载大量列表、状态、配置和操作
- 所有主要矩形区域默认使用圆角矩形
- 适当加入暗色点缀，形成视觉焦点，而不是整页深色化
- 窗口缩小时仍然可用，不出现挤压、重叠和错位

一句话概括：

> 采用 `Apple B2` 混合风格：浅色主场、单蓝色交互、圆角卡片、适度暗色点缀，同时保留工具型 UI 的清晰信息结构和高频操作效率。

## 2. 适用范围

适合以下类型的 `Python GUI`：

- 本地管理工具
- 文件管理工具
- 数据查看与操作工具
- 对象存储、批处理、实验管理、配置管理类工具
- 左侧列表 + 右侧详情 / 功能侧栏的桌面应用
- 需要在 Windows 环境下稳定运行的 `PySide6` 应用

不适合直接套用的场景：

- 强营销型落地页
- 游戏 UI
- 纯展示型大屏
- 极简小弹窗工具

## 3. 总体设计原则

### 3.1 先结构，后视觉

UI 首先要解决工作流问题，再解决好看问题。

优先确认：

- 哪个区域是主工作区
- 哪些操作是高频操作
- 哪些信息只是辅助摘要
- 哪些配置不需要一直展开
- 窗口变小时如何重排

视觉风格必须服务这些结构，而不是反过来让装饰抢走主任务。

### 3.2 美观和信息兼顾

不要把 Apple 风格理解成“大留白 + 少信息”。工具型桌面应用仍然要让用户快速扫读、比较和操作。

推荐做法：

- 主列表保留足够宽度和行数
- 右侧只放补充信息，不重复主列表
- 操作区清晰分组，但不要堆成巨大按钮墙
- 信息卡片要克制，避免每个字段都做成大卡片

### 3.3 所有主要矩形默认圆角化

以下元素默认都要使用圆角矩形：

- 主内容卡片
- 内嵌分区
- 输入框
- 下拉框
- 按钮
- 表格外轮廓
- 状态栏顶部边界
- 只读链接框

经验值：

- 主卡片圆角：`22px` 到 `24px`
- 内嵌分区圆角：`18px` 到 `20px`
- 输入框和按钮圆角：`14px` 到 `16px`
- 状态栏圆角：`16px` 到 `18px`

避免残留直角，否则会破坏整套 Apple B2 风格。

### 3.4 暗色只做点缀

默认界面不做整页暗色，而是用暗色承担少量高优先级信息。

适合使用暗色的地方：

- 顶部配置摘要 badge
- 关键状态卡
- 当前连接状态
- 需要形成视觉锚点的区域

不建议：

- 大面积深色背景
- 所有卡片都深色
- 多种深色混用
- 用深色装饰无意义区域

推荐暗色点缀方式：

- 浅色背景 + 白色卡片 + 少量深色 badge
- 深色只出现 1 到 2 个视觉锚点
- 深色区域文字必须高对比

### 3.5 统一交互主色

默认只使用一个主交互色，例如蓝色。

推荐：

- 主按钮：蓝色实心
- 普通按钮：浅灰 / 白色
- 危险按钮：浅红底 + 红色文字 / 边框
- 选中态：浅蓝背景
- 焦点态：蓝色边框

不要同时混用多套主色，否则工具 UI 会显得杂乱。

## 4. 推荐整体布局

推荐默认结构：

```text
顶部可折叠配置区
中部筛选与浏览区
主内容区：左侧主列表 + 右侧功能侧栏
底部对象 / 批量操作区
状态栏
```

这套结构适合大多数桌面工具。

### 4.1 顶部可折叠配置区

适合放：

- 连接参数
- 全局账号配置
- API Key / Token
- bucket / 项目切换
- 高级配置入口

设计规范：

- 用可折叠区域承载
- 本地已有完整配置时默认折叠
- 标题区显示摘要 badge，例如当前 bucket、连接状态
- 展开后表单支持 3 列、2 列、1 列响应式重排
- 操作按钮放在配置区内部底部

解决的问题：

- 避免连接配置长期占据首屏
- 让用户进入应用后优先看到主工作区
- 同时保留配置可见性和状态感

### 4.2 中部筛选与浏览区

适合放：

- 搜索框
- 前缀 / 路径筛选
- 状态筛选
- 刷新按钮

设计规范：

- 独立成圆角卡片
- 不和对象操作按钮混在一起
- 宽屏时一行展示
- 中等宽度时按钮换到下一行
- 小窗口时表单纵向堆叠

### 4.3 主内容区

推荐使用 `QSplitter`。

宽屏时：

- 左侧主列表占主要宽度
- 右侧功能侧栏占辅助宽度

窄屏时：

- 自动切换为上下布局
- 左侧主列表在上
- 右侧功能侧栏在下

设计规范：

- 左侧是主工作区，不要被侧栏挤压
- 侧栏放入 `QScrollArea`
- 侧栏内容多时纵向滚动，不挤压主表格
- 分割条保持透明或弱视觉

### 4.4 底部对象操作区

适合放：

- 上传
- 下载
- 删除
- 生成 URL
- 创建分享
- 停止分享
- 批量操作

设计规范：

- 独立成圆角卡片
- 放在主内容区下方，避免抢占主列表视觉主位
- 按钮按网格自动换行
- 主操作使用蓝色
- 危险操作使用红色弱警示样式

不要把操作区放在主列表上方抢视觉焦点，除非该工具的核心任务就是操作按钮而不是列表浏览。

## 5. Apple B2 视觉规范

### 5.1 基础配色

推荐色彩结构：

```text
页面背景：浅灰白
主卡片：白色
内嵌分区：极浅灰
边框：柔和浅灰
主文字：接近黑色
次级文字：中性灰
主交互：蓝色
危险操作：红色
暗色点缀：接近黑色
```

示例：

```python
COLOR_BG = "#f5f5f7"
COLOR_SURFACE = "#ffffff"
COLOR_SURFACE_ALT = "#fbfbfd"
COLOR_SURFACE_SUBTLE = "#f7f8fb"
COLOR_BORDER = "#e4e4ea"
COLOR_BORDER_SOFT = "#eff0f4"
COLOR_TEXT = "#1d1d1f"
COLOR_TEXT_MUTED = "#6e6e73"
COLOR_TEXT_SUBTLE = "#8d8d92"
COLOR_PRIMARY = "#0066cc"
COLOR_PRIMARY_SOFT = "#e8f1ff"
COLOR_DANGER = "#c43c2f"
COLOR_DANGER_SOFT = "#fff3f2"
COLOR_DARK_CARD = "#1d1d1f"
```

### 5.2 字体规范

Windows 上优先使用中文友好的字体栈：

```css
font-family: "Microsoft YaHei UI", "Segoe UI Variable", "Segoe UI";
```

原因：

- 避免中文在本地或离屏渲染时出现方块字
- 保持中英文混排稳定
- 比只使用 `Segoe UI` 更适合中文桌面工具

### 5.3 字号层级

推荐层级：

- 普通文本：`13px`
- 字段标题：`12px`
- 分区弱标题：`12px` 到 `13px`
- 主要标题：`16px`
- 状态 hero 标题：`18px`

注意：

- 不要在工具型 UI 中使用过大的标题
- 主列表和按钮内文字要紧凑但可读
- 标题样式要统一，避免同级标题粗细不同

### 5.4 卡片层级

推荐分两层：

- 主卡片：页面上的大区域
- 内嵌分区：主卡片内部的小功能块

不要做多层卡片嵌套。

错误示例：

```text
外层大卡片 -> 内层卡片 -> 表格自身又有边框
```

推荐示例：

```text
对象列表标题 + QTableWidget 自身承担唯一主卡片轮廓
右侧外层主卡片 + 内部两个圆角分区
```

### 5.5 对齐规范

相邻区域的圆角框上边界要对齐。

尤其注意：

- 左侧对象列表圆角框
- 右侧链接操作圆角框
- 底部操作区圆角框
- 筛选区圆角框

如果左侧有标题，右侧没有标题，可以给右侧增加不可见顶部占位，让真正的圆角框对齐，而不是只让外层容器对齐。

## 6. 表格与主列表规范

### 6.1 主列表是主工作区

如果应用的核心是浏览、选择、比较对象，主表格必须拥有最大视觉空间。

推荐：

- 左侧主列表占主内容区大部分宽度
- 表格行高略宽松
- 表头可点击排序
- 表格外轮廓为圆角
- 不再用额外外层卡片包住表格

### 6.2 避免双层矩形

常见问题：

```text
对象列表外层卡片是圆角
表格自己又有一层矩形边框
结果形成双层框
```

解决方式：

- 删除外层表格卡片
- 保留独立标题
- 让 `QTableWidget` 自己承担唯一主卡片轮廓
- 表头顶角、表格边框、表格底角统一圆角

### 6.3 表头排序是一等能力

如果表格是主工作区，默认必须支持：

- 点击表头排序
- 升序 / 降序切换
- 刷新后保留排序状态
- 刷新后保留选中项

排序不要按显示文本。

应按真实值排序：

- `Size` 按字节数
- 日期按时间值
- 数字按整数或浮点数
- 文本按标准化字符串

推荐实现：

- 自定义 `QTableWidgetItem`
- 保存 `sort_value`
- 在 `__lt__` 中比较真实值

### 6.4 表格视觉

推荐：

- 表头背景使用极浅灰
- 表头文字使用次级灰
- 行分隔线轻一些
- 选中态使用浅蓝背景
- 不显示强网格线
- 行高略高于默认值

不要：

- 强黑边框
- 高饱和表头
- 密集网格线
- 选中态过深导致文字难读

## 7. 右侧侧栏规范

右侧侧栏只做补充，不做第二个主工作区。

适合放：

- 链接操作
- 当前可复制 URL
- 当前可复制分享链接
- 容量概览
- 当前对象的少量摘要
- 统计信息

不建议放：

- 与左侧表格完全重复的对象基础字段
- 大量按钮
- 多个互相抢视觉的状态卡

已验证的问题：

- 右侧重复展示“当前对象状态”会让界面主次混乱
- 底部操作区放在主内容上方会抢主列表视觉

解决方式：

- 右侧只保留 `链接操作 + 容量概览`
- 对象基础信息交给左侧主列表
- 对象操作下移到底部

## 8. 标题与文字样式规范

### 8.1 同级标题必须统一

常见问题：

```text
“对象列表”是 16px 黑色粗体
“对象操作”是 12px 灰色标题
两者视觉等级不一致
```

解决方式：

- 同级区域标题使用同一弱标题样式
- 主工作区标题不要因为是 `QLabel` 就比 `QGroupBox` 标题更重
- 可以定义 `PaneTitle` 专门给主面板标题使用

推荐：

```css
QLabel#PaneTitle {
    color: muted;
    font-size: 13px;
    font-weight: 600;
    padding-left: 18px;
}
```

### 8.2 文案职责

界面文字应该短而准确：

- 标题说明区域
- 按钮说明动作
- 状态说明当前结果
- 占位符提示输入内容

不要在界面里写长篇使用说明。复杂说明放文档。

## 9. 响应式设计规范

### 9.1 必须支持的行为

每个新的 `PySide6` 工具默认要支持：

- 配置区自动换列
- 筛选区自动换行
- 按钮区自动换行
- 主内容区横向分栏切换为纵向分栏
- 右侧侧栏滚动
- 小窗口下不出现内容重叠

### 9.2 推荐断点

断点按实际拥挤程度调整，不要机械照抄。

可参考：

- `>= 1320px`：配置区 3 列
- `980px - 1319px`：配置区 2 列
- `< 980px`：配置区 1 列
- `>= 1180px`：主内容左右分栏
- `< 1180px`：主内容上下分栏

按钮区可按宽度从 5 列逐步降到 1 列。

### 9.3 不要固定死的内容

避免：

- 表单永远 3 列
- 按钮永远一行
- 右侧栏永远固定宽度
- 表格列宽全部固定
- 容量圆环永远固定大尺寸

这些会导致缩小时 UI 挤压失真。

## 10. 折叠区规范

适合折叠：

- 连接配置
- 高级设置
- 低频修改的全局参数

不适合折叠：

- 主列表
- 高频操作按钮
- 当前任务状态

推荐：

- 独立封装 `collapsible_section.py`
- 标题区显示箭头
- 标题区显示摘要 badge
- 支持默认折叠
- 展开内容使用响应式表单布局

经验规则：

- 本地已有完整配置时，启动默认折叠
- 初次配置或配置缺失时，默认展开

## 11. 按钮与操作规范

### 11.1 按钮分级

推荐：

- 主按钮：蓝色实心
- 普通按钮：白色或浅灰
- 危险按钮：浅红底 + 红色文字
- 禁用按钮：浅灰 + 弱文字

### 11.2 操作布局

高频对象操作应该集中，但不要挤在一行。

推荐：

- 使用 `QGridLayout`
- 根据窗口宽度重建列数
- 每个按钮同宽
- 保持固定高度和圆角

### 11.3 危险操作

危险操作必须视觉区分。

例如：

- 删除选中对象
- 停止分享
- 清空配置

同时需要弹窗确认的操作，应只对真正不可逆或高风险操作弹窗，不要所有操作都弹窗。

## 12. 状态与反馈规范

### 12.1 反馈分层

推荐三层：

- 状态栏：普通状态变化
- 轻量状态文字：当前 URL / 分享 / 容量
- 弹窗：错误、确认删除、严重失败

不要把所有反馈都做成弹窗。

### 12.2 状态刷新

状态刷新要避免打断用户上下文：

- 表格刷新后保留选中项
- 表格刷新后保留排序状态
- 倒计时刷新只更新显示，不重新请求远程资源
- 右侧详情随选中项同步

## 13. 后台任务规范

耗时操作必须异步。

包括：

- 网络请求
- 上传
- 下载
- 删除
- 远程状态查询
- 大列表刷新

推荐结构：

```text
pyside6_ui/
├── main_window.py
├── state.py
├── tasks.py
├── theme.py
└── widgets/
```

职责划分：

- `main_window.py`：顶层布局、事件绑定、状态同步
- `state.py`：界面状态
- `tasks.py`：后台任务封装
- `theme.py`：主题常量和样式表
- `widgets/`：可复用组件

操作期间：

- 禁用相关按钮
- 状态栏显示当前任务
- 成功后恢复按钮
- 失败后恢复按钮并给出错误

## 14. 文件结构规范

推荐结构：

```text
.
├── app.py
├── requirements.txt
├── config_manager.py
├── 项目概览.md
└── pyside6_ui/
    ├── __init__.py
    ├── main_window.py
    ├── state.py
    ├── tasks.py
    ├── theme.py
    └── widgets/
        ├── collapsible_section.py
        ├── object_table.py
        ├── detail_panel.py
        └── storage_ring.py
```

说明：

- `app.py` 只做应用入口
- 不要把所有 UI 代码塞进入口文件
- 主题统一放在 `theme.py`
- 表格、详情面板、容量环、折叠区都拆成组件
- 项目概览应记录设计规范、问题和修正过程

## 15. 已验证问题与解决方案

### 15.1 配置区占据首屏

问题：

- 连接配置长期展开，占用大量首屏空间
- 用户每次打开工具都先看到配置，而不是主任务

解决：

- 配置区做成可折叠组件
- 配置完整时默认折叠
- 标题区用 badge 显示当前 bucket 和连接状态

### 15.2 窗口缩小时布局挤爆

问题：

- 早期静态大窗口布局在缩小时按钮、侧栏、表格互相挤压

解决：

- 配置区自动换列
- 筛选区自动换行
- 操作按钮区自动换行
- 主内容区从左右分栏切到上下分栏
- 侧栏放入滚动区

### 15.3 右侧信息重复

问题：

- 右侧“当前对象状态”与左侧对象列表重复
- 视觉上形成两个主工作区，用户不知道该看哪里

解决：

- 删除重复对象状态卡
- 右侧只保留链接操作和容量概览
- 左侧对象列表继续承担主信息展示

### 15.4 对象操作区抢主视觉

问题：

- 操作区如果放在主内容区上方，会比对象列表更显眼

解决：

- 对象操作区下移到主内容区下方
- 主列表成为首要视觉中心

### 15.5 双层矩形轮廓

问题：

- 外层卡片包表格，表格自己又有边框，形成双层矩形

解决：

- 左侧改为 `标题 + 单张表格主卡片`
- 由 `QTableWidget` 自身承担唯一轮廓
- 不重新引入外层表格卡片

### 15.6 圆角卡片上边界不对齐

问题：

- 左侧对象列表有标题，右侧链接操作没有同等标题槽
- 两侧圆角卡片顶部不在同一水平线

解决：

- 统一标题样式
- 给右侧增加不可见顶部占位
- 对齐真正的圆角框顶部，而不是只对齐外层容器
- 用离屏实例化测量控件坐标，确认差值为 `0`

### 15.7 同级标题样式不一致

问题：

- `QLabel` 标题和 `QGroupBox` 标题样式不同
- 同级区域视觉层级混乱

解决：

- 定义统一弱标题样式
- “对象列表”和“对象操作”使用接近的标题层级

### 15.8 中文字体显示方块

问题：

- 离屏渲染或部分 Windows 环境中中文显示为方块

解决：

- 在 `theme.py` 中优先使用 `Microsoft YaHei UI`
- 自绘组件也显式设置中文友好字体
- 验证时区分字体渲染问题和真实布局问题

## 16. 代码样式片段

### 16.1 主题样式骨架

```python
APP_STYLESHEET = f"""
QWidget {{
    background: transparent;
    color: {COLOR_TEXT};
    font-family: "Microsoft YaHei UI", "Segoe UI Variable", "Segoe UI";
    font-size: 13px;
}}
QMainWindow {{
    background: {COLOR_BG};
}}
QFrame#SurfaceCard,
QGroupBox#SurfaceCard,
QWidget#SurfaceCard {{
    background-color: {COLOR_SURFACE};
    border: 1px solid {COLOR_BORDER};
    border-radius: 24px;
}}
QFrame#InsetPanel,
QWidget#InsetPanel {{
    background-color: {COLOR_SURFACE_SUBTLE};
    border: 1px solid {COLOR_BORDER_SOFT};
    border-radius: 20px;
}}
QPushButton {{
    border-radius: 16px;
    font-weight: 600;
}}
QPushButton#PrimaryButton {{
    background-color: {COLOR_PRIMARY};
    color: #ffffff;
}}
QPushButton#DangerButton {{
    background-color: {COLOR_DANGER_SOFT};
    color: {COLOR_DANGER};
}}
"""
```

### 16.2 主内容区布局骨架

```python
self.main_splitter = QSplitter(Qt.Horizontal)
self.main_splitter.setChildrenCollapsible(False)

left_panel = QWidget()
left_layout = QVBoxLayout(left_panel)
left_layout.setContentsMargins(0, 0, 0, 0)
left_layout.setSpacing(12)

table_title = QLabel("对象列表")
table_title.setObjectName("PaneTitle")
left_layout.addWidget(table_title)
left_layout.addWidget(self.table, 1)

right_panel = QWidget()
right_layout = QVBoxLayout(right_panel)
right_layout.setContentsMargins(0, 0, 0, 0)
right_layout.setSpacing(0)

title_spacer = QWidget()
title_spacer.setFixedHeight(9)
right_layout.addWidget(title_spacer)
right_layout.addWidget(self.detail_scroll, 1)

self.main_splitter.addWidget(left_panel)
self.main_splitter.addWidget(right_panel)
self.main_splitter.setStretchFactor(0, 4)
self.main_splitter.setStretchFactor(1, 2)
```

### 16.3 响应式重排骨架

```python
def resizeEvent(self, event) -> None:
    super().resizeEvent(event)
    self._rebuild_responsive_layouts()

def _rebuild_responsive_layouts(self) -> None:
    width = self.width()
    self._rebuild_config_fields(width)
    self._rebuild_filter_layout(width)
    self._rebuild_action_layout(width)
    self._update_splitter_orientation(width)

def _update_splitter_orientation(self, width: int) -> None:
    orientation = Qt.Horizontal if width >= 1180 else Qt.Vertical
    if self.main_splitter.orientation() == orientation:
        return
    self.main_splitter.setOrientation(orientation)
```

## 17. 验证规范

每次完成新的 PySide6 UI 或重要布局调整后，至少执行：

```powershell
python -m py_compile app.py pyside6_ui/main_window.py pyside6_ui/theme.py
```

推荐再做：

- 离屏实例化主窗口
- 短事件循环 smoke test
- `app.py` 离屏启动检查
- 截图验证
- 关键控件坐标测量

布局对齐问题不要只靠肉眼，必要时测量控件坐标。

例如：

```text
左侧表格圆角框 y = 269
右侧链接操作圆角框 y = 269
差值 = 0
```

## 18. 设计检查清单

### 18.1 风格

- 是否采用浅色主场
- 是否统一主交互色
- 是否所有主要矩形都是圆角
- 是否有适度暗色点缀
- 是否避免整页单调或过度装饰

### 18.2 布局

- 主工作区是否明确
- 配置区是否应该折叠
- 操作区是否抢主视觉
- 左右卡片上边界是否对齐
- 窗口缩小时是否可用

### 18.3 表格

- 是否支持表头排序
- 是否按真实值排序
- 是否保留排序状态
- 是否保留选中项
- 是否避免双层矩形

### 18.4 侧栏

- 是否只放补充信息
- 是否删除与主列表重复的内容
- 是否可滚动
- 是否没有抢主工作区

### 18.5 交互

- 耗时操作是否异步
- 操作期间按钮是否禁用
- 状态反馈是否清晰
- 弹窗是否只用于必要场景

### 18.6 文档

- 是否记录设计规范
- 是否记录遇到的问题
- 是否记录如何解决
- 文档路径是否使用相对路径
- 正式说明是否使用中文

## 19. 可直接复用的一句话模板

以后设计新的 `PySide6` Python UI 时，可以默认套用：

> 采用 `Apple B2` 混合风格，以浅色背景、白色圆角卡片、单蓝色交互、柔和边框和少量暗色状态点缀建立产品感；布局采用 `顶部可折叠配置区 + 中部筛选区 + 左侧主表格 + 右侧功能侧栏 + 底部对象操作区`，主表格承担核心信息展示，右侧只保留补充操作和状态摘要，并支持窗口缩小时自动换列、按钮换行和主内容区横竖切换。
