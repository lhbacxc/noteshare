# 项目概览

## 项目简介

本项目是一个基于 Python 的本地桌面工具，用于管理 Cloudflare R2 存储桶中的对象文件。

当前桌面端界面已经统一切换为 `PySide6`，目标是让用户在本地通过更现代、可响应的图形界面完成常见对象存储操作，而不需要频繁手写脚本或命令。

当前界面主线已经进一步升级为 `Apple B2` 混合风格：保留工具型桌面应用的高频操作效率，同时通过更统一的浅色主场、单蓝色交互、圆角卡片层级和更明确的主次布局，让界面更接近产品化工具而不是传统后台 GUI。

除基础的 R2 文件管理能力外，项目当前还集成了：

- 文件级预签名 URL 管理
- 可手动停止的分享链接能力
- 当前 bucket 容量概览展示

## 当前能力

项目当前已经支持以下功能：

- 保存并加载 R2 连接配置
- 测试连接
- 加载并切换 bucket
- 打开 GUI 时自动加载当前配置 bucket 的文件列表
- 按前缀浏览对象
- 按关键字过滤对象列表
- 点击表头按列排序，支持升序和降序切换
- 为单个文件设置默认预签名 URL 过期秒数
- 通过文件选择或拖拽上传文件到指定 bucket
- 打开 GUI 后支持将本地文件拖入主窗口、对象列表或对象操作区触发上传
- 上传文件时展示进度条、已上传大小、总大小、百分比和当前上传速度
- 多文件上传时按本次任务总字节数汇总进度，并显示当前正在上传第几个文件
- 上传过程中支持手动暂停当前上传任务，并可点击继续上传从断点恢复
- 上传过程中支持手动取消当前上传任务
- 上传仍在进行时关闭窗口会先询问是否取消上传并关闭
- 暂停上传或网络异常上传失败时会保留本地断点状态，再次继续上传或重新上传同一本地文件到同一对象 Key 时自动续传
- 上传前会在后台计算本地文件 MD5；本地文件路径变化或文件改名但内容不变时，可通过 MD5 找回并继续原断点上传
- 如果 MD5 命中的断点属于原对象 Key，而当前文件名生成了新的对象 Key，会继续沿用断点中的原对象 Key 续传
- 暂停后关闭客户端不会清理断点；下次重启会在上传状态区显示可继续上传的文件
- 取消上传会清理本次断点，下次上传同一文件时重新开始
- 上传失败、断点取消失败和后台任务失败会写入本地日志，便于后续排查
- 可在连接配置区选择日志等级：全部、警告、错误，默认只记录错误
- 可在连接配置区保存 WebDAV 账号信息和远端配置路径
- 支持将当前本地 `config.json` 上传同步到 WebDAV
- 支持从 WebDAV 下载远端 `config.json`，校验成功后覆盖本地配置并刷新界面
- 本地 GUI 上传流程未额外限制单文件大小，当前实现下单文件实际上限约为 `4.995 TiB`
- 下载选中对象到本地
- 删除单个或多个对象
- 生成预签名 URL
- 复制预签名 URL 到剪贴板
- 在文件列表显示最近一次 URL 的到期时间和剩余时间
- 每 5 秒自动重算一次剩余时间显示，不重新请求 R2
- 创建可撤销分享链接
- 停止单个文件最近一次可撤销分享
- 复制可撤销分享链接到剪贴板
- 预签名 URL 与可撤销分享链接可独立创建，互不要求前置创建
- 停止可撤销分享仅影响对应分享 token，不影响已生成的预签名 URL
- 在 GUI 中配置 `Worker Base URL` 和 `Worker Admin Token`
- 通过 Cloudflare Worker + KV + R2 实现“可手动停止”的分享访问链路
- 在 GUI 中展示当前 bucket 已用空间与免费层暂定总空间 `10GB`
- 使用圆环 + 文本摘要展示当前 bucket 容量
- 连接配置区支持折叠
- 连接配置区支持折叠摘要展示，可直接显示当前 bucket 与连接状态
- 窗口缩小时支持响应式重排，避免按钮区和右侧栏挤压失真
- 对象列表作为左侧主工作区展示，并以独立标题 + 单张表格主卡片的方式呈现
- 对象操作区独立保留在主内容区下方，便于突出对象列表主体地位
- 右侧侧栏当前仅保留“链接操作”和“容量概览”，不再重复显示左侧列表已可见的对象状态信息

## 技术栈

- Python 3.10
- PySide6
- boto3
- Cloudflare Workers
- Cloudflare KV
- Cloudflare R2
- TypeScript
- Wrangler
- miniconda

## 项目结构

```text
.
├── app.py
├── app_logger.py
├── r2_client.py
├── config_manager.py
├── webdav_sync.py
├── upload_resume_store.py
├── worker_client.py
├── requirements.txt
├── cf_cloud.ico
├── NoteShareR2.spec
├── 打包NoteShareR2.bat
├── 启动R2工具.bat
├── 项目概览.md
├── pyside6_ui/
│   ├── __init__.py
│   ├── main_window.py
│   ├── state.py
│   ├── tasks.py
│   ├── theme.py
│   └── widgets/
│       ├── collapsible_section.py
│       ├── detail_panel.py
│       ├── object_table.py
│       └── storage_ring.py
├── worker/
│   ├── package.json
│   ├── package-lock.json
│   ├── tsconfig.json
│   ├── wrangler.jsonc
│   ├── README.md
│   └── src/
│       └── index.ts
└── docs/
    └── superpowers/
        └── specs/
            └── 2026-05-29-pyside6-ui-migration-design.md
            └── 2026-05-29-pyside6-apple-b2-redesign.md
            └── 2026-06-05-upload-progress-speed-design.md
            └── 2026-06-05-resumable-upload-design.md
            └── 2026-06-05-upload-stop-busy-progress-design.md
            └── 2026-06-05-cancel-stored-resumable-upload-design.md
            └── 2026-06-06-upload-md5-resume-identity-design.md
            └── 2026-06-06-upload-error-logging-design.md
            └── 2026-06-06-webdav-config-sync-design.md
```

## 关键文件说明

### `app.py`

当前项目唯一的桌面应用入口文件，负责创建 `QApplication`、加载图标并启动 `PySide6` 主窗口。

### `pyside6_ui/main_window.py`

负责主窗口布局、全局交互协调、状态刷新、后台任务回调和主要按钮事件绑定。

当前还承担以下关键交互逻辑：

- 启动时自动刷新当前配置 bucket 的文件列表
- 连接配置折叠与展开
- 连接配置摘要 badge 同步显示当前 bucket 与连接状态
- 响应式布局重排
- 维持 `连接配置 -> 筛选与浏览 -> 对象列表/右侧侧栏 -> 对象操作` 的主界面顺序
- 表格筛选与排序状态保持
- 右侧分享/容量侧栏刷新
- URL 倒计时本地刷新
- 上传、下载、删除、分享等业务动作调度
- 拖拽上传事件接收、拖入文件校验与复用现有上传任务链路
- 上传进度状态区刷新，包括总进度、当前文件、已上传大小和当前网速显示
- 主界面状态提示刷新，包括启动自动加载、对象数量、后台任务和上传状态的内联提示
- 上传暂停、继续、取消状态管理，以及上传中关闭窗口前的确认拦截
- 暂停/取消上传收尾阶段的忙碌态进度反馈，避免用户误以为客户端卡住
- 上传断点续传状态接入，暂停后点击继续上传可恢复已完成分片进度，网络失败后重新上传同一文件和对象 Key 也可续传
- 上传前后台计算文件 MD5，并用 MD5 断点身份识别移动路径或改名但内容不变的本地文件
- 日志等级配置接入，并在上传失败、断点取消失败和通用后台任务失败时写入本地日志
- WebDAV 配置同步入口接入，支持上传本地配置和下载远端配置后刷新界面

补充说明：

- 当前主窗口标题已固定为 `NoteShare R2 管理工具`
- 对象列表当前采用“独立标题 + 单张表格主卡片”的最终结构，避免双层矩形轮廓
- 状态提示已从底部状态栏迁移到对象列表标题行右侧，避免底部额外露出半截圆角条
- 对象操作区已下移到主内容区下方，避免抢占主表格的视觉主位
- 对象操作区底部新增上传状态区，默认显示“待上传”，上传中展示进度条、网速和“暂停上传”/“取消上传”按钮，暂停或失败后可点击“继续上传”
- 点击“暂停上传”或“取消上传”后，进度条会切换为忙碌态并显示收尾说明，后台完成后再回到已暂停或已取消状态

### `app_logger.py`

负责本地日志系统配置与日志等级转换。

当前主要承担：

- 将界面中的“全部 / 警告 / 错误”转换为内部日志等级
- 初始化 `logs/noteshare.log`
- 使用滚动日志文件，避免单个日志文件长期无限增长
- 向主窗口提供统一 logger，用于记录上传和后台任务错误

### `pyside6_ui/theme.py`

负责 `PySide6` 界面的全局主题常量与 `APP_STYLESHEET`。

当前主要承担：

- Apple `B2` 风格的浅色主场配色
- 单蓝色交互体系
- 卡片、内嵌分区、输入框、按钮、表格、状态栏的统一圆角与边框规则
- 上传进度状态区和进度条样式
- 中文优先字体栈配置，避免本地渲染时出现方块字问题

### `pyside6_ui/widgets/object_table.py`

负责对象表格展示。

当前支持：

- 左侧主工作区中的“对象列表”主体展示
- 由 `QTableWidget` 自身承担对象列表主卡片轮廓，避免外层卡片重复包裹
- 表头点击排序
- 行选中与多选
- 关键列宽自适应
- 在刷新数据后保留当前排序方式
- 更宽松的行高与更克制的选中态样式

### `pyside6_ui/widgets/detail_panel.py`

负责右侧侧栏展示。

当前保留以下两块信息：

- 链接操作
- bucket 容量概览

补充说明：

- 已移除与左侧对象列表重复的“当前对象状态”展示卡
- 当前侧栏为外层主卡片 + 内部“链接操作”/“容量概览”两个圆角分区
- 右侧仅保留当前文件的 URL / 分享操作摘要与 bucket 容量信息

### `pyside6_ui/widgets/collapsible_section.py`

负责可折叠区域组件，当前用于连接配置区。

补充说明：

- 当前已支持在标题区显示摘要 badge，用于同步呈现当前 bucket 和连接状态

### `pyside6_ui/widgets/storage_ring.py`

负责容量圆环绘制。

补充说明：

- 当前已跟随 Apple `B2` 风格统一调整颜色、字号和圆环粗细，使其更像产品状态组件而不是传统技术仪表盘

### `pyside6_ui/tasks.py`

负责后台任务封装，统一管理耗时操作的线程执行与信号回调，避免界面主线程被阻塞。

补充说明：

- 当前后台任务已支持可选进度信号，上传任务可通过该信号把进度快照安全传回主线程
- 普通任务仍沿用原有 `started` / `result` / `error` / `finished` 链路，不需要额外传入进度回调

### `r2_client.py`

负责封装所有与 Cloudflare R2 交互的逻辑，包括：

- 测试连接
- 列出 bucket
- 列出对象
- 上传文件
- 下载文件
- 删除对象
- 生成预签名 URL

补充说明：

- 仍保留普通 `upload_file` 封装，可使用 `boto3` 的 `upload_file` 与可选 `Callback`
- 当前 GUI 上传主链路使用可断点续传的 S3 兼容 multipart API，包括 `create_multipart_upload`、`list_parts`、`upload_part` 和 `complete_multipart_upload`
- 断点续传会按已完成 part 跳过重复传输，并把已恢复字节数计入 GUI 总进度
- 单文件上传能力取决于 `boto3` 自动 multipart 上传与 Cloudflare R2 对象大小限制
- 按当前链路口径，单次非分片上传上限为 `5 GiB`，单文件实际上限约为 `4.995 TiB`

### `upload_resume_store.py`

负责保存和清理本地断点续传状态。

当前主要承担：

- 在程序运行目录维护 `upload_resume_state.json`
- 新断点按 `endpoint + bucket + object key + 文件大小 + MD5` 生成上传会话 key
- 兼容旧断点的 `endpoint + bucket + object key + 本地路径 + 文件大小 + 文件修改时间` 会话 key
- 分块计算本地文件 MD5，用于上传前匹配同内容断点
- 保存 R2 multipart upload 的 `upload_id`、分片大小和已完成 part 信息
- 上传成功后清理对应断点状态
- 状态文件损坏时自动忽略并重建，避免阻塞正常上传

### `config_manager.py`

负责本地配置文件的读取、清洗和保存。

当前配置默认保存在根目录下的 `config.json` 中。

当前还负责清洗 WebDAV 同步配置字段，并暴露统一配置清洗入口，供远端配置下载后复用同一套校验逻辑。

### `webdav_sync.py`

负责 WebDAV 配置同步逻辑。

当前主要承担：

- 校验 WebDAV URL 与远端配置文件路径
- 使用标准库发起 WebDAV `MKCOL` / `PUT` / `GET` 请求
- 上传前逐级创建远端目录
- 将本地 `config.json` 上传到 WebDAV
- 从 WebDAV 下载远端配置 JSON，并在写入本地前复用 `config_manager.py` 的配置清洗逻辑

### `worker_client.py`

负责桌面应用与 Cloudflare Worker 管理接口之间的 HTTP 通信，包括：

- 创建分享
- 停止分享
- 查询分享状态
- 统一处理 Worker 返回错误

### `worker/src/index.ts`

Cloudflare Worker 入口文件，负责：

- 管理接口鉴权
- 创建分享记录
- 停止分享记录
- 查询分享状态
- 公开访问分享链接
- 从 R2 读取对象并按类型决定 `inline` 或 `attachment`

### `NoteShareR2.spec`

当前 `PyInstaller` 单文件打包配置文件，已以 `PySide6` 版本的 `app.py` 为入口。

### `打包NoteShareR2.bat`

一键打包脚本，使用 `noteshare` 环境中的 `python.exe` 调用 `PyInstaller`。

## 配置文件说明

项目会在根目录使用 `config.json` 保存本地配置，主要包含：

- `account_id`
- `access_key_id`
- `secret_access_key`
- `endpoint_url`
- `worker_base_url`
- `worker_admin_token`
- `webdav_url`
- `webdav_username`
- `webdav_password`
- `webdav_remote_path`
- `default_bucket`
- `url_expire_seconds`
- `log_level`
- `recent_buckets`
- `object_url_settings`

说明：

- 当前实现以本地易用性优先
- `secret_access_key` 会以明文形式保存在 `config.json` 中
- `worker_admin_token` 也会以明文形式保存在 `config.json` 中
- `webdav_password` 也会以明文形式保存在 `config.json` 中
- `webdav_remote_path` 默认值为 `noteshare/config.json`
- `log_level` 保存当前日志等级，可选值为 `all`、`warning`、`error`，默认值为 `error`
- 每个文件的默认过期秒数与最近一次预签名 URL 状态也会保存在本地配置中
- 文件级 URL / 分享状态按 `bucket + object key` 维度保存
- 预签名 URL 状态与可撤销分享状态分别保存，互不覆盖
- 当前每个文件只持久化最近一次可撤销分享记录
- 适合单机、自用场景

断点续传状态独立保存在根目录的：

```text
upload_resume_state.json
```

说明：

- 该文件只保存未完成上传的 multipart 恢复信息，不保存 R2 密钥
- 该文件已加入 `.gitignore`，不应提交到 Git
- 上传成功或取消上传后，对应记录会自动删除
- 暂停上传后关闭客户端不会删除对应记录，重启后仍可继续上传
- 网络异常导致失败时会保留对应记录，便于下次重试续传
- 新断点会记录 `content_md5`，本地文件路径变化或文件改名但内容不变时仍可被识别
- 旧路径维度断点仍可兼容读取；成功续传或再次保存后会写入新的 MD5 信息
- R2 multipart upload 的 `upload_id` 绑定 `bucket + object key`，因此文件改名后命中旧断点时会继续上传到断点中的原对象 Key
- 如果本地文件内容变化导致 MD5 不一致，旧断点不会被继续使用，会从头创建新的上传
- 本地 `upload_resume_state.json` 没有主动过期时间；但 Cloudflare R2 默认会在 7 天后自动中止未完成的 multipart upload，因此超过远端生命周期后只能重新上传

运行日志保存在：

```text
logs/noteshare.log
```

说明：

- 该文件用于记录上传失败、断点取消失败、通用后台任务失败等排查信息
- 日志内容使用 UTF-8 编码
- 日志文件会滚动保留，避免单个文件无限增长
- `logs/` 已加入 `.gitignore`，不应提交到 Git

## 运行方式

### 方式一：命令行启动

```powershell
conda activate noteshare
python app.py
```

### 方式二：双击启动

直接双击根目录下的：

```text
启动R2工具.bat
```

### 方式三：单文件 exe 启动

打包完成后，可直接运行：

```text
dist/NoteShareR2.exe
```

## 打包说明

当前推荐使用 `PyInstaller` 单文件方案，配置文件为：

```text
NoteShareR2.spec
```

命令行打包命令为：

```powershell
python -m PyInstaller --noconfirm --clean NoteShareR2.spec
```

也可以直接双击：

```text
打包NoteShareR2.bat
```

### 打包前提

- 已创建 `noteshare` conda 环境
- 已在该环境安装项目运行依赖
- 推荐先执行 `pip install -r requirements.txt`
- 已在该环境安装 `PyInstaller`
- 根目录中存在 `cf_cloud.ico`

### 当前打包注意事项

- 必须优先使用 `noteshare` 环境中的 `python.exe` 进行打包
- 打包时必须将该环境的以下路径放到当前进程 `PATH` 前部：
  - 环境根目录
  - `Library/bin`
  - `Scripts`
- `boto3`、`botocore` 的网络访问链路依赖 `ssl` 运行库，打包时需要固定 `libssl-3-x64.dll` 与 `libcrypto-3-x64.dll`
- 加载 bucket 时底层会走 XML 解析链路，打包时需要固定 `libexpat.dll`，并显式加入 `pyexpat`、`xml.parsers.expat`
- 图标文件 `cf_cloud.ico` 需要同时用于 `PyInstaller` 的 `icon` 配置与程序运行时窗口图标加载

## 当前实现特点

- 统一使用 `PySide6` 桌面界面
- GUI 已升级为 Apple `B2` 混合风格，更接近产品化桌面工具
- 一套密钥下支持切换多个 bucket
- 通过后台线程执行网络请求，尽量避免界面卡死
- 支持连接配置折叠与响应式布局
- 支持连接配置折叠摘要与当前 bucket / 连接状态 badge 展示
- 支持按文件维度管理默认过期秒数与最近一次 URL 状态
- 支持按文件维度管理最近一次可撤销分享状态
- 支持启动时自动恢复并加载上次使用的 bucket 列表
- 支持打开 GUI 后直接将本地文件拖入主窗口、对象列表或对象操作区上传
- 支持上传进度条、实时上传速度和多文件总进度显示
- 支持上传中手动暂停、继续和取消，并在窗口关闭前提示用户处理正在进行的上传
- 支持上传断点续传恢复，暂停后点击继续上传会自动跳过已完成分片
- 支持暂停后关闭客户端仍保留断点，重启后在上传状态区显示可继续上传的文件
- 支持网络异常失败后保留断点，再次上传同一文件到同一对象 Key 时自动续传
- 取消上传会清理本次断点，下次上传同一文件会重新开始
- 支持本地日志文件记录上传与后台任务错误，并可在连接配置区选择日志等级
- 支持通过 WebDAV 手动上传和下载本地配置文件，便于在新环境快速恢复 R2 / Worker / URL 状态配置
- 支持每 5 秒本地动态刷新 URL 剩余时间显示
- 支持通过 Worker + KV + R2 实现可撤销分享
- 支持对同一文件分别独立生成预签名 URL 与可撤销分享链接
- 支持在 GUI 中直接查看当前 bucket 容量占用情况
- 支持以“对象列表”为左侧主工作区、以底部“对象操作”为独立操作区的主次布局
- 支持以“独立标题 + 单张表格主卡片”展示对象列表，避免双层矩形
- 右侧侧栏已收缩为“链接操作 + 容量概览”两块信息，避免重复展示对象状态

## 本轮 UI 更新记录

### Apple `B2` 风格重设计

本轮已围绕 `pyside6UI` 分支完成一轮正式的 `PySide6` 界面风格升级，目标是保留桌面工具效率的前提下，让界面更接近 Apple 官网 / Apple Store 的克制产品感。

本次新增设计文档：

- `docs/superpowers/specs/2026-05-29-pyside6-apple-b2-redesign.md`

本次核心改动文件：

- `pyside6_ui/theme.py`
- `pyside6_ui/main_window.py`
- `pyside6_ui/widgets/collapsible_section.py`
- `pyside6_ui/widgets/detail_panel.py`
- `pyside6_ui/widgets/object_table.py`
- `pyside6_ui/widgets/storage_ring.py`

本次主要改动内容：

- 将全局界面主题切换为浅色主场、单蓝色交互和大圆角白卡片风格
- 重做连接配置区标题层级，并增加当前 bucket / 连接状态摘要 badge
- 优化筛选区、按钮区、表格和右侧侧栏的统一视觉语言
- 将容量圆环改成更轻量的产品状态风格
- 调整表格行高、选中态和整体留白，使对象列表更易扫读

### 后续 UI 修正

在 Apple `B2` 风格落地后，又根据实际界面效果补了 3 个结构修正：

1. 删除右侧与左侧对象列表重复的“当前对象状态”信息卡
2. 给左侧主表格补上“对象列表”标题，和其他卡片保持一致
3. 将“对象操作”区从主内容区上方调整到下方，让对象列表成为更明确的主体

### 2026-06-02 圆角与主卡片对齐修正

本轮继续围绕 Apple `B2` 风格做了一次一致性收口，重点解决默认布局下左侧“对象列表”和右侧主侧栏底层圆角卡片不对齐的问题，并统一主界面残留的直角矩形视觉。

本次主要修正：

- 将右侧侧栏改为外层统一圆角主卡片，内部再拆分“链接操作”和“容量概览”两个圆角分区
- 让左侧对象列表主卡片与右侧侧栏主卡片在默认横向布局下保持统一的底层卡片高度感
- 补齐主界面局部遗漏的圆角样式，包括只读输入框、表格表头顶角和状态栏顶角
- 继续保持右侧仅承载“链接操作 + 容量概览”，不回退为信息重复的多卡片堆叠结构

### 2026-06-02 对象列表双层矩形修正

根据实际截图又补了一轮更细的表格样式修正，目标是去掉“对象列表”卡片内部再次出现的一层表格矩形轮廓，并继续压缩残留的直角边界。

本次主要修正：

- 将左侧结构调整为“对象列表标题 + 单张表格卡片”，不再使用外层卡片再套表格卡片
- 让 `QTableWidget` 自己承担唯一的对象列表主卡片轮廓，消除双层矩形
- 将按钮、输入框、表格头等关键控件样式改成更明确的圆角背景写法，继续压缩残留直角视觉

### 2026-06-03 项目概览收口同步

在前两轮 UI 调整完成后，又对 `项目概览.md` 做了一次与代码现状对齐的收口更新，避免文档仍保留“对象列表外层卡片包裹表格”之类已经过时的描述。

本次主要同步：

- 将对象列表的最终形态明确为“独立标题 + 单张表格主卡片”
- 将右侧侧栏的最终形态明确为“外层主卡片 + 内部两个圆角分区”
- 将主题职责补充为同时覆盖卡片与内嵌分区的统一圆角规则

### 2026-06-03 对象列表标题与卡片上边界修正

根据实际截图继续修正左侧对象列表与右侧链接操作区域的视觉一致性。

本次主要修正：

- 将“对象列表”标题改为与“对象操作”标题更接近的弱化标题样式，避免左侧标题比底部操作区更重
- 在右侧侧栏外补齐与左侧标题等高的顶部占位，让左侧对象列表主卡片与右侧链接操作圆角卡片上边界对齐
- 保持对象列表仍由 `QTableWidget` 自身承担唯一主卡片轮廓，不重新引入外层嵌套卡片

### 2026-06-06 对象列表底部圆角修正

根据实际截图继续修正对象列表主卡片的边缘渲染问题。根因是 `QTableWidget` 自身已经有圆角主卡片轮廓，但内部 `viewport`、空白区域和滚动条背景仍可能按直角绘制，导致视觉上像是表格直角矩形压在底层圆角卡片上。

本次主要修正：

- 为对象列表表格的 `viewport` 增加明确对象名，便于样式表单独控制内部绘制区域
- 为对象列表表格增加控件级圆角裁剪，避免内部 `viewport`、表格内容或滚动条子控件越过主卡片圆角边界
- 在 `pyside6_ui/theme.py` 中补齐对象表格 `viewport`、交替行、角落区域和对象表格专属垂直滚动条的样式
- 将对象表格垂直滚动条轨道改为透明，只保留滑块本身，避免右上角出现突出的滑槽背景
- 保持对象列表仍是“独立标题 + 单张表格主卡片”，不新增外层卡片，也不回退为双层矩形结构
- 已通过离屏方式分别验证空白表格和带滚动条表格，确认底部圆角不再被直角内容区域覆盖

### 2026-06-06 底部状态栏移除

根据实际截图继续修正主窗口底部视觉问题。根因是 `QStatusBar` 被设置为带顶部圆角的浅色状态条，贴在窗口底部后会露出半截圆角矩形；而“已加载 8 个对象”这类状态文字本身不依赖底部状态栏，也可以放在主内容区内显示。

本次主要修正：

- 移除主窗口底部 `QStatusBar`，避免窗口底部额外出现半截圆角矩形
- 将状态提示迁移到“对象列表”标题行右侧，继续展示启动自动加载、对象数量、上传和后台任务状态
- 为新的内联状态文字补充弱提示样式，避免抢占对象列表主标题视觉层级
- 保留连接配置摘要中的状态同步逻辑，不影响顶部折叠区对当前状态的展示

### 2026-06-06 对象操作区缩放重排修正

根据实际截图继续修正主窗口最大化后再缩回原大小时，对象操作区按钮和上传状态区被挤到左侧、右侧残留大块空白的问题。根因是响应式重排会反复复用同一个 `QGridLayout`，旧列的 stretch 没有在重排前清理；当布局从 5 列切回 4 列时，第 5 个空列仍保留伸展权重，导致可见控件只占据左侧部分宽度。

本次主要修正：

- 重排 `QGridLayout` 前同步清理旧行列的 stretch 和最小尺寸，避免最大化、还原或连续缩放后残留旧列状态
- 对象操作区列数改为按 `action_card` 实际可用内容宽度计算，不再只依赖主窗口宽度
- 保持对象操作按钮最多 5 列展示，上传状态区始终跨越当前有效列数并铺满对象操作卡片
- 已通过离屏方式模拟 `1320 -> 2048 -> 1320` 连续缩放，确认上传状态区会重新铺满当前对象操作区宽度

### 2026-06-05 拖拽上传支持

本轮在现有 `PySide6` 主界面中补齐拖拽上传能力，让打开 GUI 后可以直接把本地文件拖入主窗口、对象列表或对象操作区触发上传。

本次主要改动文件：

- `pyside6_ui/main_window.py`
- `项目概览.md`

本次主要改动内容：

- 为主窗口、滚动画布、对象列表和对象操作区启用本地文件拖放接收
- 将原有“上传文件”按钮流程拆成可复用的本地文件上传入口，按钮上传和拖拽上传共用同一条后台任务链路
- 拖入文件后继续复用对象 Key 确认弹窗，默认 Key 会沿用当前前缀与本地文件名
- 支持一次拖入多个本地文件，并逐个确认对象 Key 后批量上传
- 上传完成后继续刷新对象列表，并同步更新当前 bucket 容量摘要
- 拖入文件夹或远程链接时给出明确提示，当前仅支持本地文件

### 2026-06-05 上传进度与网速显示

本轮继续补齐上传过程中的反馈能力，让大文件或多文件上传时可以直接看到上传是否仍在推进，以及当前网速大致处于什么水平。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-05-upload-progress-speed-design.md`

本次主要改动文件：

- `r2_client.py`
- `pyside6_ui/tasks.py`
- `pyside6_ui/main_window.py`
- `pyside6_ui/theme.py`
- `项目概览.md`

本次主要改动内容：

- 在“对象操作”区底部新增上传状态区，默认显示“待上传”
- 上传开始后显示当前文件名、总进度百分比、已上传大小、总大小和当前上传速度
- 多文件上传时按本次任务的总字节数汇总进度，并显示当前正在上传第几个文件
- 为 `R2Manager.upload_file` 增加可选 `progress_callback`，复用 `boto3 upload_file` 原生 `Callback` 获取增量上传字节数
- 为后台任务封装增加可选进度信号，让上传线程可以把进度快照安全传回主线程刷新 UI
- 上传失败时保留失败状态，并继续沿用原有错误弹窗提示具体原因

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`
- 已用模拟进度快照验证进度条、百分比和 `MB/s` 网速文本可正常刷新

### 2026-06-05 关闭前询问与取消上传

本轮在上传进度能力基础上补齐上传中断保护，避免大文件上传时误关窗口，也让用户可以主动停止当前上传任务。

本次主要改动文件：

- `pyside6_ui/main_window.py`
- `pyside6_ui/theme.py`
- `项目概览.md`

本次主要改动内容：

- 在上传状态区增加“取消上传”按钮，默认禁用，上传开始后启用，点击后会请求停止当前上传任务
- 为上传任务增加取消令牌，上传线程会在每个文件开始前、上传进度回调中和单文件上传完成后检查取消状态
- 取消上传时使用专门的取消状态处理，不再按普通失败弹出红色错误提示
- 上传中再次点击上传或拖入文件时提示已有上传任务正在进行，避免多个上传任务同时写入同一块进度状态
- 上传中关闭窗口会先弹出确认框；选择取消上传并关闭时，会先发出取消请求，等待后台上传任务收尾后再关闭窗口
- 为上传状态区中的取消按钮补充紧凑样式，避免进度区标题栏被按钮撑高

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`
- 已用模拟上传中状态验证取消按钮默认禁用、取消请求触发后取消令牌会被置位

### 2026-06-05 上传断点续传与暂停继续

本轮在上传取消能力基础上继续补齐断点续传与暂停继续，让大文件上传可以主动暂停后继续，也能在网络异常失败后从已完成分片处重试。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-05-resumable-upload-design.md`

本次主要改动文件：

- `r2_client.py`
- `upload_resume_store.py`
- `pyside6_ui/main_window.py`
- `.gitignore`
- `项目概览.md`

本次主要改动内容：

- 新增 `upload_resume_state.json` 作为本地断点续传状态文件，并加入 `.gitignore`
- 在上传状态区新增“暂停上传”和“继续上传”按钮，暂停后可直接点击继续上传
- 启动时会读取本地断点状态，若发现仍存在的本地文件，会在上传状态区显示“发现可继续上传”
- 上传同一本地文件到同一 `bucket + object key` 时，会自动读取未完成 multipart upload 状态
- R2 上传链路改为可恢复的 multipart upload，支持列出远端已完成 part 并跳过重复上传
- 上传成功后自动清理对应断点状态；取消上传会清理本次断点并尽量 abort 远端 multipart upload
- 网络异常导致上传失败时保留断点状态，便于点击继续上传或下次重新选择同一文件和对象 Key 时续传
- 已暂停任务在客户端重启后仍可直接点击“取消上传”，清理本地断点并尝试停止远端未完成 multipart upload
- 进度区在恢复到已有分片时显示“断点续传”，开始传后续未完成分片后切回“正在上传”
- 暂停/取消上传不再弹出 `An HTTP Client raised an unhandled exception` 类型错误窗，而是回到专门的暂停/取消状态
- 暂停后再关闭客户端会等待暂停状态落盘后关闭，不会把已暂停任务误处理成取消上传
- 本地断点状态不会主动过期；远端未完成 multipart upload 默认 7 天后由 R2 中止，届时继续上传会自动回退为重新上传

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py upload_resume_store.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`
- 已用模拟断点续传进度快照验证进度条和“断点续传”文案可正常刷新
- 已用离屏方式验证暂停、继续、取消按钮的默认状态与暂停/取消文案
- 已用离屏方式验证“点击暂停后关闭窗口”会等待暂停收尾并保留可继续任务
- 已验证 botocore `HTTPClientError` 包装暂停/取消异常时会还原为专门上传中断状态

### 2026-06-05 暂停与取消上传忙碌进度反馈

本轮针对暂停/取消上传时“标题已变化但进度条静止，容易让用户误以为客户端卡住”的体验问题做了一次小范围修正。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-05-upload-stop-busy-progress-design.md`

本次主要改动文件：

- `pyside6_ui/main_window.py`
- `项目概览.md`

本次主要改动内容：

- 点击“暂停上传”后，将上传进度条切换为忙碌态，并提示正在等待当前分片响应与保存断点
- 点击“取消上传”后，将上传进度条切换为忙碌态，并提示正在停止上传与清理本次断点
- 暂停/取消收尾阶段忽略后续普通上传进度快照，避免标题被刷新回“正在上传”
- 上传完成、失败、已暂停或已取消后统一恢复确定进度条，避免忙碌态残留
- 记录最近一次确定进度值，暂停完成后仍保留点击暂停前的大致进度

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py upload_resume_store.py upload_control.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`
- 已用离屏模拟验证暂停/取消请求会进入忙碌态，最终暂停状态会恢复原进度，最终取消状态会恢复到 0

### 2026-06-05 重启后取消已暂停上传

本轮修正了暂停上传后关闭客户端、下次重启只支持“继续上传”而“取消上传”无反应的问题。根因是重启后恢复的是静态断点任务状态，没有后台上传线程；原有取消逻辑只处理正在上传的线程，因此直接返回。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-05-cancel-stored-resumable-upload-design.md`

本次主要改动文件：

- `pyside6_ui/main_window.py`
- `项目概览.md`

本次主要改动内容：

- 启动时发现可继续上传任务后，同时启用“继续上传”和“取消上传”
- 上传暂停完成后也保留“取消上传”可用，用户可在不继续上传的情况下放弃该断点任务
- 当没有后台上传线程但存在可继续任务时，“取消上传”会进入专门的静态断点清理流程
- 静态断点清理流程会删除本地 `upload_resume_state.json` 中对应记录，并尝试 abort 远端 multipart upload
- 如果远端 abort 失败，会显示“取消上传失败”，保留本地断点，方便稍后重试取消或继续上传

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py upload_resume_store.py upload_control.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`
- 已用离屏模拟验证重启后的可继续任务会启用“取消上传”
- 已用离屏模拟验证静态断点取消成功后会清空恢复任务状态，并禁用“继续上传”与“取消上传”

### 2026-06-06 MD5 断点身份

本轮继续完善断点续传的本地文件识别方式，解决“本地文件移动路径”或“内容不变但改了文件名”后再次上传会被当成新任务的问题。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-06-upload-md5-resume-identity-design.md`

本次主要改动文件：

- `upload_resume_store.py`
- `r2_client.py`
- `pyside6_ui/main_window.py`
- `README.md`
- `项目概览.md`

本次主要改动内容：

- 上传前在后台线程分块计算本地文件 MD5，并在上传状态区显示“正在校验文件”
- 新断点状态按 `endpoint + bucket + object key + 文件大小 + MD5` 生成会话 key
- 继续兼容旧的 `本地路径 + 文件大小 + 文件修改时间` 断点 key，旧断点被继续使用后会补写 MD5 信息
- 再次上传同内容文件时，优先通过 MD5 查找未完成断点；本地路径变化或文件改名但内容不变时仍可续传
- 当 MD5 命中的断点属于原对象 Key，而当前文件名生成了新的对象 Key 时，继续沿用断点中的原对象 Key 续传，避免违反 R2/S3 multipart upload 的 `upload_id` 绑定关系
- 上传成功、取消上传和静态断点取消时，都会按 MD5 与旧路径兼容规则清理本地断点状态

本次验证结果：

- 已通过 `python -m py_compile app.py r2_client.py upload_resume_store.py upload_control.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已用临时文件验证相同内容在不同路径、不同文件名下可通过 MD5 命中同一个断点
- 已用临时文件验证同内容但当前对象 Key 不同时，会回退使用断点中的原对象 Key
- 已用临时文件验证 MD5 断点可按内容身份清理，不残留本地 session
- 已通过离屏方式实例化 `NoteShareMainWindow`，并验证 MD5 校验阶段的上传状态文案可正常刷新

### 2026-06-06 上传错误日志与日志等级

本轮补齐本地日志记录能力，解决上传失败后只能看当场弹窗、缺少可回溯错误信息的问题。现在可以在连接配置区选择日志等级，并把上传与后台任务错误写入本地日志文件。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-06-upload-error-logging-design.md`

本次主要改动文件：

- `app_logger.py`
- `config_manager.py`
- `pyside6_ui/main_window.py`
- `.gitignore`
- `项目概览.md`

本次主要改动内容：

- 新增 `app_logger.py`，集中管理日志等级、日志文件路径和滚动日志 handler
- 在 `config.json` 中新增 `log_level` 字段，默认值为 `error`
- 在连接配置区新增“日志等级”下拉框，支持“全部 / 警告 / 错误”
- 保存配置后立即重新应用日志等级
- 上传任务开始、完成、暂停和取消按 `INFO` 写入日志；当等级选择“全部”时可看到完整过程
- 上传失败、静态断点取消失败和通用后台任务失败按 `ERROR` 写入日志，并保留异常堆栈
- 日志文件固定保存在 `logs/noteshare.log`，`logs/` 已加入 `.gitignore`

本次验证结果：

- 已通过 `python -m py_compile app.py app_logger.py config_manager.py r2_client.py upload_resume_store.py upload_control.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过离屏方式实例化 `NoteShareMainWindow`，并确认日志等级下拉框可加载
- 已实际写入测试错误日志，确认 `logs/noteshare.log` 可生成

### 2026-06-06 WebDAV 配置同步

本轮增加 WebDAV 配置同步能力，让本地 `config.json` 可以上传到 WebDAV，也可以从 WebDAV 下载恢复到本地，便于换设备或重装后快速恢复 R2、Worker、URL 状态和日志等级等配置。

本次新增设计文档：

- `docs/superpowers/specs/2026-06-06-webdav-config-sync-design.md`

本次主要改动文件：

- `config_manager.py`
- `webdav_sync.py`
- `pyside6_ui/main_window.py`
- `项目概览.md`

本次主要改动内容：

- 在 `config.json` 中新增 `webdav_url`、`webdav_username`、`webdav_password`、`webdav_remote_path` 字段
- 在连接配置区新增 WebDAV URL、用户名、密码和远端路径输入框
- 在连接配置区新增“上传配置”和“下载配置”按钮
- 上传配置时先保存当前表单，再把最新本地 `config.json` 上传到 WebDAV
- 下载配置时先从 WebDAV 读取远端 JSON，通过现有配置清洗逻辑校验成功后再覆盖本地 `config.json`
- 下载成功后自动刷新界面字段、bucket 下拉框、日志等级和连接配置摘要
- WebDAV 请求使用 Python 标准库实现，不新增运行依赖；上传前会按远端路径逐级创建目录

本次验证结果：

- 已通过 `python -m py_compile app.py app_logger.py config_manager.py r2_client.py upload_resume_store.py upload_control.py webdav_sync.py pyside6_ui/main_window.py pyside6_ui/tasks.py pyside6_ui/theme.py`
- 已通过本地临时 WebDAV 小服务验证 `MKCOL`、`PUT`、`GET` 和下载后配置清洗写入链路
- 已通过离屏方式实例化 `NoteShareMainWindow`，并确认 WebDAV 输入框和同步按钮可加载

### 本轮踩坑与修正

- 在本地离屏渲染验证时，曾出现中文字体显示为方块字的问题；最终通过在 `theme.py` 和 `storage_ring.py` 中提高 `Microsoft YaHei UI` 的字体优先级解决
- 初版 Apple `B2` 侧栏曾加入“当前对象状态”卡，但实际使用中与左侧列表重复，后续已删除并简化为更聚焦的侧栏结构
- 本轮离屏验证第一次使用了 bash 风格的 `python <<'PY'` 写法，在 PowerShell 下会被解析为非法重定向；已改为 PowerShell here-string 管道给 `python -` 后通过验证
- 本轮 WebDAV 验证没有依赖真实第三方账号，而是用本地临时服务模拟标准 WebDAV 方法，避免把真实配置上传到外部服务

## 当前限制

- 当前仅支持一套密钥配置
- 未实现多账号配置档案
- 未实现对象重命名和移动
- 未实现真正的目录树浏览，只支持前缀方式浏览
- 当前每个文件只保留最近一次可撤销分享记录
- 如果同一文件先后创建过多个可撤销分享，当前“停止分享”默认只会停止本地最近一次记录对应的 token，较早创建且仍有效的旧分享不会被自动一并撤销
- 当前未实现分享历史列表、访问统计、分享密码和批量停止分享
- 当前未提供独立的断点任务管理界面，断点状态会在上传状态区自动提示并按暂停、取消和网络失败语义在后台复用与清理
- WebDAV 配置同步当前为手动上传/下载，不做自动同步、冲突合并或版本历史管理
- 当前“所有 bucket 合计”仍为占位文案，尚未实现真实统计
- 当前总空间上限仍固定为免费层暂定值 `10GB`

## 当前 Cloudflare 资源状态

当前可撤销分享功能对应的 Cloudflare 资源已经完成：

- Worker：`noteshare-share-worker`
- R2 bucket：`notelink`
- KV Namespace：用于保存分享记录

说明：

- Worker 的部署配置保存在 `worker/wrangler.jsonc`
- 管理密钥通过 `wrangler secret put ADMIN_TOKEN` 写入 Cloudflare
- 桌面应用通过本地 `config.json` 保存 Worker 地址与管理密钥

## 后续可扩展方向

- 增加多账号配置切换
- 增加更安全的密钥存储方式
- 增加对象重命名与移动
- 增加更完整的目录树视图
- 增加分页加载和更大的对象列表支持
- 增加每个文件的多条分享历史记录
- 增加分享记录列表和状态筛选
- 增加批量停止分享
- 增加访问统计、分享备注和密码访问
- 增加所有 bucket 合计容量的真实统计
- 增加上传任务历史列表与断点状态管理界面
- 增加 WebDAV 自动同步、同步前备份和配置版本历史
