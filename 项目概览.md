# 项目概览

## 项目简介

本项目是一个基于 Python 的本地桌面工具，用于管理 Cloudflare R2 存储桶中的对象文件。

当前工具使用 `Tkinter` 构建 GUI，使用 `boto3` 访问 R2 的 S3 兼容接口，目标是让用户在本地通过图形界面完成常见对象存储操作，而不需要频繁手写脚本或命令。

除基础的 R2 文件管理能力外，项目当前还集成了：

- 文件级预签名 URL 管理
- 可手动停止的分享链接能力
- 当前 bucket 容量概览展示

## 当前能力

项目当前已经支持以下功能：

- 保存并加载 R2 连接配置
- 测试连接
- 加载并切换 bucket
- 打开 GUI 时自动加载当前配置 bucket 的文件列表
- 按前缀浏览对象
- 按关键字过滤对象列表
- 为单个文件设置默认预签名 URL 过期秒数
- 上传文件到指定 bucket
- 下载选中对象到本地
- 删除单个或多个对象
- 生成预签名 URL
- 复制预签名 URL 到剪贴板
- 在文件列表显示最近一次 URL 的到期时间和剩余时间
- 选中单个文件时显示当前仍有效的历史预签名 URL
- 每 5 秒自动重算一次剩余时间显示，不重新请求 R2
- 创建可撤销分享链接
- 停止单个文件最近一次可撤销分享
- 复制可撤销分享链接到剪贴板
- 在 GUI 中配置 `Worker Base URL` 和 `Worker Admin Token`
- 选中单个文件时显示当前仍有效的最近一次分享链接
- 通过 Cloudflare Worker + KV + R2 实现“可手动停止”的分享访问链路
- 在 GUI 中展示当前 bucket 已用空间与免费层暂定总空间 `10GB`
- 使用圆环 + 文本摘要展示当前 bucket 容量
- 在对象列表区使用“左侧文件列表，右侧容量侧栏”的两列布局展示容量信息
- 在极小占用比例场景下将已用比例显示为 `<0.1%`
- 在容量圆环中明确区分浅灰底环与青绿色已用弧线

## 技术栈

- Python 3.10
- Tkinter
- boto3
- Cloudflare Workers
- Cloudflare KV
- Cloudflare R2
- TypeScript
- Wrangler
- miniconda

## 项目结构

```text
.
├── app.py
├── .gitignore
├── gui.py
├── r2_client.py
├── config_manager.py
├── requirements.txt
├── worker_client.py
├── README.md
├── 项目概览.md
├── 启动R2工具.bat
├── worker/
│   ├── package.json
│   ├── package-lock.json
│   ├── tsconfig.json
│   ├── wrangler.jsonc
│   ├── README.md
│   └── src/
│       └── index.ts
└── docs/
    └── superpowers/
        └── specs/
            ├── 2026-05-24-r2-gui-design.md
            ├── 2026-05-24-r2-file-level-presigned-url-design.md
            ├── 2026-05-24-stop-link-worker-design.md
            ├── 2026-05-25-r2-bucket-storage-usage-design.md
            ├── 2026-05-25-r2-storage-sidebar-layout-design.md
            └── 2026-05-25-r2-storage-ring-display-fix-design.md
```

## 关键文件说明

### `app.py`

项目入口文件，负责创建主窗口并启动 GUI 主循环。

### `gui.py`

负责图形界面布局、表单交互、对象列表展示、按钮事件绑定，以及后台线程任务调度。

当前还承担以下关键交互逻辑：

- 启动时自动刷新当前配置 bucket 的文件列表
- 按文件维度展示默认过期秒数与 URL 剩余时间
- 选中单个文件时自动显示仍有效的历史预签名 URL
- 选中单个文件时自动显示仍有效的最近一次分享链接
- 每 5 秒本地重算一次剩余时间显示
- 创建、停止和复制可撤销分享链接
- 使用主线程 UI 队列处理后台线程回调，降低 GUI 假死风险
- 统计当前 bucket 总容量
- 在右侧容量侧栏绘制圆环并显示容量摘要
- 在存在 `prefix` 的情况下，额外获取一次当前 bucket 全量对象列表，仅用于容量统计
- 对极小占用比例做 `<0.1%` 文案显示处理
- 通过浅灰底环与青绿色前景弧线区分未使用与已使用容量

### `r2_client.py`

负责封装所有与 Cloudflare R2 交互的逻辑，包括：

- 测试连接
- 列出 bucket
- 列出对象
- 上传文件
- 下载文件
- 删除对象
- 生成预签名 URL

### `config_manager.py`

负责本地配置文件的读取、清洗和保存。

当前配置默认保存在根目录下的 `config.json` 中。

目前已经扩展支持：

- `worker_base_url`
- `worker_admin_token`
- `last_share_token`
- `last_share_url`
- `last_share_status`
- `last_share_created_at`
- `last_share_expires_at`

### `worker_client.py`

负责 GUI 与 Cloudflare Worker 管理接口之间的 HTTP 通信，包括：

- 创建分享
- 停止分享
- 查询分享状态
- 统一处理 Worker 返回错误

### `worker/src/index.ts`

Cloudflare Worker 入口文件，负责：

- 管理接口鉴权
- 创建分享记录
- 停止分享记录
- 查询分享状态
- 公开访问分享链接
- 从 R2 读取对象并按类型决定 `inline` 或 `attachment`

## 配置文件说明

项目会在根目录使用 `config.json` 保存本地配置，主要包含：

- `account_id`
- `access_key_id`
- `secret_access_key`
- `endpoint_url`
- `worker_base_url`
- `worker_admin_token`
- `default_bucket`
- `url_expire_seconds`
- `recent_buckets`
- `object_url_settings`

说明：

- 当前实现以本地易用性优先
- `secret_access_key` 会以明文形式保存在 `config.json` 中
- `worker_admin_token` 也会以明文形式保存在 `config.json` 中
- 每个文件的默认过期秒数与最近一次预签名 URL 状态也会保存在本地配置中
- 文件级 URL / 分享状态按 `bucket + object key` 维度保存
- 适合单机、自用场景

## 运行方式

### 方式一：命令行启动

```powershell
conda activate noteshare
python app.py
```

### 方式二：双击启动

直接双击根目录下的：

```text
启动R2工具.bat
```

## 典型使用流程

1. 打开 GUI。
2. 填写 `Account ID`、`Access Key ID`、`Secret Access Key` 和 `Endpoint URL`。
3. 点击“保存配置”。
4. 点击“测试连接”。
5. 点击“加载 Bucket”。
6. 选择目标 bucket。
7. 如有需要，输入前缀并刷新文件列表。
8. 按需执行上传、下载、删除、生成预签名 URL 等操作。
9. 如需使用可撤销分享，补充 `Worker Base URL` 和 `Worker Admin Token`。
10. 在对象列表右侧查看当前 bucket 容量概览。

## 当前实现特点

- 依赖较少，部署轻量
- GUI 直观，适合本地日常使用
- 一套密钥下支持切换多个 bucket
- 通过后台线程执行网络请求，尽量避免界面卡死
- 支持按文件维度管理默认过期秒数与最近一次 URL 状态
- 支持按文件维度管理最近一次可撤销分享状态
- 支持启动时自动恢复并加载上次使用的 bucket 列表
- 支持每 5 秒本地动态刷新 URL 剩余时间显示
- 支持通过 Worker + KV + R2 实现可撤销分享
- 支持在 GUI 中直接查看当前 bucket 容量占用情况
- 对象列表区已采用“左列表 + 右容量栏”的布局，减少容量组件对文件列表高度的挤压
- 容量圆环已按“浅灰底环 + 青绿色已用弧线”的方式展示

## 当前限制

- 当前仅支持一套密钥配置
- 未实现多账号配置档案
- 未实现对象重命名、移动和拖拽上传
- 未实现真正的目录树浏览，只支持前缀方式浏览
- 当前每个文件只保留最近一次可撤销分享记录
- 当前未实现分享历史列表、访问统计、分享密码和批量停止分享
- 当前“所有 bucket 合计”仍为占位文案，尚未实现真实统计
- 当前总空间上限仍固定为免费层暂定值 `10GB`

## 本次改动概览

本次围绕“bucket 容量概览显示”完成了以下改动：

- 在 `gui.py` 中新增当前 bucket 容量统计与圆环展示逻辑
- 在 `gui.py` 中新增对象列表区右侧容量侧栏
- 将原本位于列表上方的容量摘要区改为“左列表 + 右侧栏”的两列布局
- 缩小容量圆环尺寸，降低界面占用
- 在存在 `prefix` 的情况下额外请求一次当前 bucket 全量对象列表，仅用于保证容量统计口径始终代表整个 bucket
- 修正极小占用比例场景下的文案显示，改为 `<0.1%`
- 修正容量圆环显示层逻辑，明确区分底环颜色与已用弧线颜色
- 新增容量展示设计文档、侧栏布局调整设计文档与圆环显示修正设计文档

## 本次改动文件与内容

### `gui.py`

主要改动：

- 新增当前 bucket 容量摘要状态字段
- 新增容量圆环绘制方法
- 新增容量摘要刷新方法
- 新增上传成功、删除成功后的本地即时容量更新
- 将对象列表区改为左右两列布局
- 将容量概览移动到右侧容量栏
- 为极小占用比例增加 `<0.1%` 文案格式化
- 为圆环增加浅灰底环与青绿色前景弧线区分

### `docs/superpowers/specs/2026-05-25-r2-bucket-storage-usage-design.md`

记录“当前 bucket 容量展示”功能的设计方案，包括：

- 展示目标
- 容量口径
- 刷新策略
- 边界处理

### `docs/superpowers/specs/2026-05-25-r2-storage-sidebar-layout-design.md`

记录“右侧容量栏布局调整”方案，包括：

- 两列布局结构
- 右栏压缩策略
- 视觉尺寸约束
- 验证范围

### `docs/superpowers/specs/2026-05-25-r2-storage-ring-display-fix-design.md`

记录“容量圆环显示修正”方案，包括：

- 百分比文案规则
- 底环与前景弧线绘制规则
- 极小占用比例下的显示策略

## 当前 Cloudflare 资源状态

当前可撤销分享功能对应的 Cloudflare 资源已经完成：

- Worker：`noteshare-share-worker`
- R2 bucket：`notelink`
- KV Namespace：用于保存分享记录

说明：

- Worker 的部署配置保存在 `worker/wrangler.jsonc`
- 管理密钥通过 `wrangler secret put ADMIN_TOKEN` 写入 Cloudflare
- GUI 通过本地 `config.json` 保存 Worker 地址与管理密钥

## 后续可扩展方向

- 增加多账号配置切换
- 增加更安全的密钥存储方式
- 增加对象重命名与移动
- 增加拖拽上传
- 增加更完整的目录树视图
- 增加分页加载和更大的对象列表支持
- 增加每个文件的多条分享历史记录
- 增加分享记录列表和状态筛选
- 增加批量停止分享
- 增加访问统计、分享备注和密码访问
- 增加所有 bucket 合计容量的真实统计
