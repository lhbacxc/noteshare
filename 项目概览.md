# 项目概览

## 项目简介

本项目是一个基于 Python 的本地桌面工具，用于管理 Cloudflare R2 存储桶中的对象文件。

当前工具采用 `Tkinter` 构建 GUI，采用 `boto3` 访问 R2 的 S3 兼容接口，目标是让用户在本地通过图形界面完成常见对象存储操作，而不需要频繁手写脚本或命令。

## 当前能力

项目当前已经支持以下功能：

- 保存并加载 R2 连接配置
- 测试连接
- 加载和切换 bucket
- 打开 GUI 时自动加载当前配置 bucket 的文件列表
- 按前缀浏览对象
- 按关键字过滤对象列表
- 为单个文件设置默认预签名 URL 过期秒数
- 上传文件到指定 bucket
- 下载选中对象到本地
- 删除单个或多个对象
- 生成预签名 URL
- 复制预签名 URL 到剪贴板
- 在文件列表显示最近一次 URL 的到期时间和剩余时间
- 选中单个文件时显示当前仍有效的历史预签名 URL
- 每 5 秒自动重算一次剩余时间显示，不重新请求 R2
- 创建可撤销分享链接
- 停止单个文件最近一次可撤销分享
- 复制可撤销分享链接到剪贴板
- 在 GUI 中配置 `Worker Base URL` 和 `Worker Admin Token`
- 选中单个文件时显示当前仍有效的最近一次分享链接
- 通过 Cloudflare Worker + KV + R2 实现“可手动停止”的分享访问链路

## 技术栈

- Python 3.10
- Tkinter
- boto3
- Cloudflare Workers
- Cloudflare KV
- Cloudflare R2
- TypeScript
- Wrangler
- miniconda

## 项目结构

```text
.
├─ app.py
├─ .gitignore
├─ gui.py
├─ r2_client.py
├─ config_manager.py
├─ config.json
├─ requirements.txt
├─ worker_client.py
├─ README.md
├─ 项目概览.md
├─ 启动R2工具.bat
├─ worker/
│  ├─ package.json
│  ├─ package-lock.json
│  ├─ tsconfig.json
│  ├─ wrangler.jsonc
│  ├─ README.md
│  └─ src/
│     └─ index.ts
└─ docs/
   └─ superpowers/
      └─ specs/
         ├─ 2026-05-24-r2-gui-design.md
         ├─ 2026-05-24-r2-file-level-presigned-url-design.md
         └─ 2026-05-24-stop-link-worker-design.md
```

## 关键文件说明

### `app.py`

项目入口文件，负责创建主窗口并启动 GUI 主循环。

### `gui.py`

负责图形界面布局、表单交互、对象列表展示、按钮事件绑定，以及后台线程任务调度。

当前还承担以下关键交互逻辑：

- 启动时自动刷新当前配置 bucket 的文件列表
- 按文件维度展示默认过期秒数与 URL 剩余时间
- 选中单个文件时自动显示仍有效的历史预签名 URL
- 选中单个文件时自动显示仍有效的最近一次分享链接
- 每 5 秒本地重算一次剩余时间显示
- 创建、停止和复制可撤销分享链接
- 使用主线程 UI 队列处理后台线程回调，降低 GUI 假死风险

这是当前项目中最核心的界面层文件。

### `r2_client.py`

负责封装所有与 Cloudflare R2 交互的逻辑，包括：

- 测试连接
- 列出 bucket
- 列出对象
- 上传文件
- 下载文件
- 删除对象
- 生成预签名 URL

### `config_manager.py`

负责本地配置文件的读取、清洗和保存。

当前配置默认保存在根目录下的 `config.json` 中。

目前已经扩展支持：

- `worker_base_url`
- `worker_admin_token`
- `last_share_token`
- `last_share_url`
- `last_share_status`
- `last_share_created_at`
- `last_share_expires_at`

### `requirements.txt`

记录项目依赖。当前主要依赖为 `boto3`。

### `启动R2工具.bat`

Windows 下的一键启动脚本，双击后会直接查找 `noteshare` 环境中的 `pythonw.exe` 来启动 GUI。

### `README.md`

面向使用者的中文说明文档，包含环境准备、启动方式、配置说明和常见问题。

### `worker_client.py`

负责 GUI 与 Cloudflare Worker 管理接口之间的 HTTP 通信，包括：

- 创建分享
- 停止分享
- 查询分享状态
- 统一处理 Worker 返回错误

### `worker/src/index.ts`

Cloudflare Worker 入口文件，负责：

- 管理接口鉴权
- 创建分享记录
- 停止分享记录
- 查询分享状态
- 公开访问分享链接
- 从 R2 读取对象并按类型决定 `inline` 或 `attachment`

### `docs/superpowers/specs/2026-05-24-r2-gui-design.md`

项目设计文档，记录了本工具的目标、结构、功能边界、异常处理和运行方式。

### `docs/superpowers/specs/2026-05-24-r2-file-level-presigned-url-design.md`

文件级预签名 URL 管理功能设计文档，记录了单文件过期秒数、历史 URL 状态持久化、列表显示和底部 URL 联动等改造设计。

### `docs/superpowers/specs/2026-05-24-stop-link-worker-design.md`

可撤销分享链接功能设计文档，记录了 GUI、Worker、KV、R2 和管理接口的整体设计。

## 配置文件说明

项目会在根目录使用 `config.json` 保存本地配置，主要包含：

- `account_id`
- `access_key_id`
- `secret_access_key`
- `endpoint_url`
- `worker_base_url`
- `worker_admin_token`
- `default_bucket`
- `url_expire_seconds`
- `recent_buckets`
- `object_url_settings`

说明：

- 当前实现以本地易用性优先。
- `secret_access_key` 会以明文形式保存在 `config.json` 中。
- `worker_admin_token` 也会以明文形式保存在 `config.json` 中。
- 每个文件的默认过期秒数与最近一次预签名 URL 状态也会保存在本地配置中。
- 文件级 URL / 分享状态按 `bucket + object key` 维度保存。
- 适合单机、自用场景。

## 运行方式

### 方式一：命令行启动

```powershell
conda activate noteshare
python app.py
```

### 方式二：双击启动

直接双击根目录下的：

```text
启动R2工具.bat
```

## 典型使用流程

1. 打开 GUI。
2. 填写 `Account ID`、`Access Key ID`、`Secret Access Key` 和 `Endpoint URL`。
3. 点击“保存配置”。
4. 点击“测试连接”。
5. 点击“加载 Bucket”。
6. 选择目标 bucket。
7. 之后重新打开 GUI 时，工具会自动尝试加载当前配置 bucket 的文件列表。
8. 如有需要，先为某个文件单独设置默认过期秒数。
9. 按需执行上传、下载、删除、生成预签名 URL 等操作。
10. 生成 URL 后，列表会显示到期时间与剩余时间，底部会在文件仍有效时显示历史 URL。
11. 如需可手动停止的分享，填写 `Worker Base URL` 和 `Worker Admin Token`。
12. 选中单个文件后点击“创建可撤销分享”。
13. 分享成功后，底部会显示最近一次仍有效的分享链接。
14. 如需立即停止分享，点击“停止分享”。

## 当前实现特点

- 依赖较少，部署轻量
- GUI 直观，适合本地日常使用
- 一套密钥下支持切换多个 bucket
- 通过后台线程执行网络请求，尽量避免界面卡死
- 支持按文件维度管理默认过期秒数与最近一次 URL 状态
- 支持按文件维度管理最近一次可撤销分享状态
- 支持启动时自动恢复并加载上次使用的 bucket 列表
- 支持每 5 秒本地动态刷新 URL 剩余时间显示
- 支持通过 Worker + KV + R2 实现可撤销分享
- 支持保留传统预签名 URL 能力与可撤销分享能力并行使用

## 当前限制

- 当前仅支持一套密钥配置
- 未实现多账号配置档案
- 未实现对象重命名、移动和拖拽上传
- 未实现真正的目录树浏览，只支持前缀方式浏览
- 当前每个文件只保留最近一次可撤销分享记录
- 当前未实现分享历史列表、访问统计、分享密码和批量停止分享

## 本次改动概览

本次围绕“可撤销分享链接”能力完成了以下改动：

- 在 `gui.py` 中新增 Worker 配置项、分享按钮、分享链接展示和停止分享逻辑。
- 在 `config_manager.py` 中扩展本地配置结构，支持保存 Worker 配置和文件级分享状态。
- 新增 `worker_client.py` 作为 GUI 到 Worker 的 HTTP 客户端。
- 新增 `worker/` 目录，完成 Cloudflare Worker 工程、KV/R2 绑定配置和部署说明。
- 在 `r2_client.py` 中补充文本文件上传与预签名访问时的 `Content-Type` / `charset` 处理，修复 UTF-8 文本浏览器乱码问题。
- 在 `.gitignore` 中补充本地配置、Wrangler 状态目录和 Worker 本地依赖的忽略规则。

## 本次改动文件与内容

### `gui.py`

主要改动：

- 新增 `Worker Base URL` 与 `Worker Admin Token` 配置输入框。
- 新增“创建可撤销分享”“停止分享”“复制分享链接”按钮。
- 新增底部“当前选中文件有效分享链接”显示框。
- 扩展文件级状态读取逻辑，支持 `last_share_*` 字段。
- 增加后台线程到主线程的 UI 队列，降低 Tkinter 回调导致的假死风险。
- 修复 `Z` 结尾 ISO 时间无法正确解析，导致分享链接已创建但不显示的问题。

### `config_manager.py`

主要改动：

- 扩展全局配置字段：
  - `worker_base_url`
  - `worker_admin_token`
- 扩展文件级配置字段：
  - `last_share_token`
  - `last_share_url`
  - `last_share_status`
  - `last_share_created_at`
  - `last_share_expires_at`
- 新增分享状态清洗逻辑，确保本地配置结构稳定。

### `worker_client.py`

新增文件，主要职责：

- 调用 `POST /api/shares`
- 调用 `POST /api/shares/revoke`
- 调用 `GET /api/shares/:token`
- 统一封装 Worker 返回错误
- 为请求补充更接近浏览器的请求头，降低被 Cloudflare `1010` 拦截的概率

### `r2_client.py`

主要改动：

- 上传对象时根据文件扩展名补充 `ContentType`
- 文本类型自动附加 `charset=utf-8`
- 生成预签名 URL 时补充 `ResponseContentType`
- 对文本类型补充 `ResponseContentDisposition=inline`

### `worker/wrangler.jsonc`

主要改动：

- 配置 Worker 名称 `noteshare-share-worker`
- 绑定 KV Namespace `SHARES`
- 绑定 R2 Bucket `FILES`
- 写入当前实际 bucket 名 `notelink`

### `worker/src/index.ts`

新增 Worker 入口，主要实现：

- `POST /api/shares`
- `POST /api/shares/revoke`
- `GET /api/shares/:token`
- `GET /s/:token`
- Bearer Token 鉴权
- KV 中保存分享记录
- 从 R2 读取对象并按类型返回
- 已停用 / 已过期 / 不存在统一返回 `404`

### `worker/README.md`

新增 Worker 部署说明，包含：

- 依赖安装
- `wrangler.jsonc` 配置
- `ADMIN_TOKEN` 设置
- Worker 部署步骤
- GUI 如何对接 Worker

### `worker/package.json` 与 `worker/package-lock.json`

主要改动：

- 移除项目本地 `wrangler` 依赖
- 改为使用全局 `wrangler`
- 保留 Worker 端 TypeScript 类型依赖

### `.gitignore`

主要改动：

- 忽略 `config.json`
- 忽略 `.wrangler/`
- 忽略 `worker/node_modules/`
- 忽略 `worker/.wrangler/`

## 本次遇到的错误与解决方式

### 1. UTF-8 文本通过预签名 URL 访问时浏览器乱码

现象：

- `txt` 文件本身是 UTF-8 编码
- 通过预签名 URL 在浏览器中打开后中文乱码

原因：

- 上传对象时未写入明确的 `Content-Type`
- 预签名 URL 返回时也未显式覆盖 `ResponseContentType`

解决方式：

- 在 `r2_client.py` 中为文本类型自动补充 `text/plain; charset=utf-8`
- 生成预签名 URL 时补充 `ResponseContentType`
- 对文本类型追加 `inline`

### 2. 全局 `wrangler` 不可用，只能在项目内 `npx wrangler`

现象：

- 本地执行 `wrangler login` 提示找不到命令

原因：

- `wrangler` 只安装在 `worker/node_modules` 中

解决方式：

- 改为全局安装 `wrangler`
- 从 `worker/package.json` 中移除本地 `wrangler` 依赖
- 保留项目对全局 `wrangler` 的调用方式

### 3. 创建可撤销分享第一次失败后，重新操作 GUI 假死

现象：

- 创建分享失败后，关闭重开 GUI 再尝试，界面出现卡死或异常弹窗

原因：

- 后台线程异常通过 `lambda` 延迟传回主线程时，异常变量 `exc` 已失效
- Tkinter 跨线程回调方式不够稳

解决方式：

- 在 `gui.py` 中把异常对象绑定到 `lambda` 默认参数
- 新增主线程 UI 队列，统一由主线程消费后台回调

### 4. 创建可撤销分享时出现 Cloudflare `403 / Error 1010`

现象：

- GUI 调用 Worker 管理接口时收到 `403`
- 弹窗显示 `Error 1010: Access denied`

原因：

- Python 标准库 `urllib` 默认请求头较像脚本流量，可能被 Cloudflare 边缘层拦截

解决方式：

- 在 `worker_client.py` 中补充：
  - `User-Agent`
  - `Accept-Language`
- 让请求更接近普通浏览器访问

### 5. 创建可撤销分享成功，但底部分享链接输入框为空

现象：

- 弹窗提示“可撤销分享链接已创建”
- 剪贴板中也有链接
- 但 GUI 中“当前选中文件有效分享链接”为空

原因：

- Worker 返回的时间是 `...Z` 结尾的 ISO UTC 时间
- 当前 GUI 解析时间时未兼容 `Z`

解决方式：

- 在 `gui.py` 的时间解析逻辑中把 `Z` 转为 `+00:00`
- 再使用 `datetime.fromisoformat(...)` 解析

## 当前 Cloudflare 资源状态

当前可撤销分享功能对应的 Cloudflare 资源已经完成：

- Worker：`noteshare-share-worker`
- R2 bucket：`notelink`
- KV Namespace：用于保存分享记录

说明：

- Worker 的部署配置保存在 `worker/wrangler.jsonc`
- 管理密钥通过 `wrangler secret put ADMIN_TOKEN` 写入 Cloudflare
- GUI 通过本地 `config.json` 保存 Worker 地址与管理密钥

## 后续可扩展方向

- 增加多账号配置切换
- 增加更安全的密钥存储方式
- 增加对象重命名与移动
- 增加拖拽上传
- 增加更完整的目录树视图
- 增加分页加载和更大的对象列表支持
- 增加每个文件的多条分享历史记录
- 增加分享记录列表和状态筛选
- 增加批量停止分享
- 增加访问统计、分享备注和密码访问
